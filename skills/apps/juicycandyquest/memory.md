# Juicy Candy Quest memory

- Package: `com.hideseek.juicycandyquest`
- Launches to `com.unity3d.player.UnityPlayerActivity`.
- Known surfaces:
  - menu with `LEVEL 1`
  - level 1 tutorial overlay with `Match 3 of the same Candy!`
- Money Time credited the game after install, launch, and the first level session.
- The `Close` tutorial button is sticky and may need repeated taps or a different interaction path later.
